<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.todoapi.mapper.TodoMapper">

    <select id="findAll" resultType="Todo">
        SELECT id, title, status, created_at
        FROM todo
        ORDER BY id ASC
    </select>

    <select id="findById" resultType="Todo">
        SELECT id, title, status, created_at
        FROM todo
        WHERE id = #{id}
    </select>

    <insert id="save" parameterType="Todo">
        INSERT INTO todo (id, title, status, created_at)
        VALUES (SEQ_TODO.NEXTVAL, #{title}, 'IN_PROGRESS', SYSDATE)
    </insert>

    <update id="updateStatus" parameterType="Todo">
        UPDATE todo
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM todo
        WHERE id = #{id}
    </delete>

</mapper>
